---
humio-core:
  enabled: true
  replicas: 3
  cp-helm-charts:
    cp-zookeeper:
      servers: 3
    cp-kafka:
      brokers: 3

humio-strix:
  enabled: true
  replicas: 10
  testOptions:
    users: 5
    gbPerDay: 200
    datasources: 5
    bulkSize: 5
    simulation: FixedRateIngestSimulation

humio-fluentbit:
  enabled: true
  es:
    tls: false
    autodiscovery: true

global:
  sharedTokens:
    perf: {performance-tests: default}
    fluentbit: {kubernetes: in-cluster}
    admin: {humio: default}
